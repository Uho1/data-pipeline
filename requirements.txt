# Core pipeline (ingest, export, auto-update)
pandas>=2.2.0
pyarrow>=17.0.0
yfinance>=1.2.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
duckdb>=1.1.0
pykrx>=1.0.51

# Toss export: web.backend.services import (export_toss_data.py -> ticker_analysis_service)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Optional (local only, install manually if needed)
# matplotlib>=3.9.0
# lightweight-charts>=2.1
# finterstellar>=0.3.2
# wrds>=3.2.0  # requires pandas<2.3, conflicts with core deps
